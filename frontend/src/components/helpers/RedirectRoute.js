import React from "react";
import { Navigate } from "react-router-dom";

function RedirectRoute({ route }) {
  // const { isUserAuth } = useAuth();
  // return isUserAuth() ? route : <Navigate to="/login" />;
}

export default RedirectRoute;
